import TransactionCard from "./TransactionCard";

const TransactionTimer = () => {
  return (
    <div className="pt-5">
      <TransactionCard />
    </div>
  );
};

export default TransactionTimer;
